RING_RTSP_PORT=8554

# RTSP credentials — must match ring-mqtt-data/config.json
RING_RTSP_USER=stream_user
RING_RTSP_PASS=change_me

# Camera ID from ring-mqtt — find it in ring-mqtt-data/go2rtc.yaml (key before _live)
RING_CAMERA_ID=your_camera_id_here
